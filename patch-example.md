# PATCH Example
Task: Add a null guard to prevent a known UI crash.
Tier: PATCH.
Controls: Rule 0, Stop Rule, targeted unit/smoke verification, evidence footer.
Escalate if the fix requires changing shared data contracts or auth/session behaviour.
